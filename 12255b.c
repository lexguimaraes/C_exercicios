#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>


int main(){
    int n;
    scanf("%d", &n);
    char palavras[100];
    for(int i = 0; i < n; i++){
        while (scanf("%[^\n]",palavras)==1){

        }
    }

}